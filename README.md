# cse134b-hw5

Includes a short reference for your web component:
1. the tag name
2. each supported attribute with its default and accepted values
3. the endpoint used
4. one usage example
Treat it as if another developer will drop your element into their page.

Your other JavaScript should also be covered in the README
along with details about your:
1. SSG setup, including templates
2. Put each topic in a clear section so we can grade it properly.

If you completed the extra credit(describe in a short paragraph how Pagefind works):
    1. What gets built
    2. Roughly how large the index is on your site
    3. Why it needs no search server.